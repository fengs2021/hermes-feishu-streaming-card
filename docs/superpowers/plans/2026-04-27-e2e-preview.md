# E2E Preview Materials Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add reproducible local visual preview materials for the streaming card flow.

**Architecture:** A standard-library Python script builds real sessions/events, uses the production renderer, and writes SVG plus Card JSON artifacts into docs. Tests guard the generator and docs.

**Tech Stack:** Python standard library, pytest, existing `CardSession`, `SidecarEvent`, and `render_card`.

---

### Task 1: Generator Test

**Files:**
- Create: `tests/unit/test_e2e_preview.py`

- [x] **Step 1: Write failing test**

Run `tools/generate_e2e_preview.py --output-dir <tmp>` and assert it writes `e2e-card-preview.svg` and `e2e-card-preview.json`.

- [x] **Step 2: Verify red**

Expected failure: generator script missing.

### Task 2: Generator and Artifacts

**Files:**
- Create: `tools/generate_e2e_preview.py`
- Create: `docs/assets/e2e-card-preview.svg`
- Create: `docs/assets/e2e-card-preview.json`

- [x] **Step 1: Implement generator**

Build thinking and completed sessions with real events, call `render_card()`, then write JSON and SVG.

- [x] **Step 2: Verify generator test**

```bash
python3 -m pytest tests/unit/test_e2e_preview.py -q
```

- [x] **Step 3: Generate committed artifacts**

```bash
python3 tools/generate_e2e_preview.py --output-dir docs/assets
```

### Task 3: Docs

**Files:**
- Create: `docs/e2e-verification.md`
- Modify: `README.md`
- Modify: `docs/testing.md`
- Modify: `TODO.md`
- Modify: `tests/unit/test_docs.py`

- [x] **Step 1: Document usage and safety**

Explain the generated SVG/JSON, regeneration command, and real Feishu smoke distinction.

- [x] **Step 2: Mark TODO**

Mark the E2E visual material item complete.

### Task 4: Verification

- [x] **Step 1: Run preview and docs tests**

```bash
python3 -m pytest tests/unit/test_e2e_preview.py tests/unit/test_docs.py -q
```

- [x] **Step 2: Run full suite**

```bash
python3 -m pytest -q
```
